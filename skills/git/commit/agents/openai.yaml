interface:
  display_name: "Git Commit"
  short_description: "Generate and create reviewed commits"
  default_prompt: "Invoke the skill `git:commit` to prepare the right staged changes, draft a reviewed commit message, then create the commit non-interactively."
