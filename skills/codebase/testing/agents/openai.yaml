interface:
  display_name: "Unit Testing"
  short_description: "Plan a unit test case system, audit existing coverage, fill the gaps, and prune"
  default_prompt: "Invoke the skill `codebase:testing` to plan a unit test case system for the production code in scope, audit what existing tests already cover, fill the missing cases, and prune redundancy."
