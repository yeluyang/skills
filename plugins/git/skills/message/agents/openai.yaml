interface:
  display_name: "Git Message"
  short_description: "Draft log-friendly commit messages"
  default_prompt: "Use $git:message to inspect the relevant diff and draft a git-log-review-friendly commit message only."
